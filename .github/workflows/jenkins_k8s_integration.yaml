# Copyright 2025 Canonical Ltd.
# See LICENSE file for licensing details.

name: jenkins-k8s integration tests

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'charms/jenkins-k8s/**'
      - '.github/workflows/jenkins_k8s_*.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      charm: ${{ steps.filter.outputs.charm }}
      workflow: ${{ steps.filter.outputs.workflow }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            charm:
              - 'charms/jenkins-k8s/**'
            workflow:
              - '.github/workflows/jenkins_k8s_*.yaml'

  integration-tests-jenkins-k8s:
    name: integration-tests-jenkins-k8s
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.charm == 'true' || needs.changes.outputs.workflow == 'true'
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@main
    secrets: inherit
    permissions:
      contents: read
      packages: write
      pull-requests: write
    with:
      identifier: jenkins-k8s
      working-directory: charms/jenkins-k8s
      test-tox-env: jenkins-k8s-integration
      upload-image: artifact
      extra-arguments: |
        --kube-config=${GITHUB_WORKSPACE}/kube-config
      modules: '["test_auth_proxy.py", "test_cos.py", "test_ingress.py", "test_jenkins.py", "test_k8s_agent.py", "test_machine_agent.py", "test_plugins_part_1.py", "test_plugins_part_2.py", "test_proxy.py", "test_upgrade.py", "test_external_agent.py", "test_haproxy_route.py"]'
      pre-run-script: |
        -c "sudo microk8s config > ${GITHUB_WORKSPACE}/kube-config
        chmod +x tests/integration/pre_run_script.sh
        ./tests/integration/pre_run_script.sh"
      juju-channel: 3/stable
      channel: 1.34-strict/stable
      microk8s-addons: "dns ingress rbac hostpath-storage registry"
      with-uv: true

  required-status-checks:
    name: Required Integration Test Status Checks - jenkins-k8s
    runs-on: ubuntu-latest
    needs:
      - integration-tests-jenkins-k8s
    if: always() && !cancelled()
    timeout-minutes: 5
    steps:
      - run: |
          [ '${{ needs.integration-tests-jenkins-k8s.result }}' = 'success' ] || [ '${{ needs.integration-tests-jenkins-k8s.result }}' = 'skipped' ] || (echo integration tests failed && false)
